# WSF program director (pointer)

The WSF program's orchestration decisions are kept in an append-only ledger on the `wsf-control-state` branch, and are read and written only with `tools/wsf-control/`.

- The procedure: `.claude/skills/wsf-program-director/SKILL.md`.
- The contract (files, phases, snapshot, findings, commands): `docs/westayfit/ops/CONTROL_STATE.md`.
- The staging release itself: `skills/wsf-staging-deploy/SKILL.md`.
