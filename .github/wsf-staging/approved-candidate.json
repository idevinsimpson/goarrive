{
  "_comment": [
    "The application commit this workflow is approved to deploy.",
    "",
    "A 40-hex app_sha input establishes SYNTAX, not approval. Every commit on",
    "every branch is 40 hex characters. This file is the approval, it lives on",
    "the default branch, and changing it is a reviewed change.",
    "",
    "The candidate checkout never supplies its own approval: this file is read",
    "from the operational checkout (the commit the workflow itself ran from),",
    "never from the application commit under deployment.",
    "",
    "Future acceptances are a one-line edit here, reviewed like any other."
  ],
  "project": "westayfit-staging",
  "approvedAppSha": "fcb83f7338c62a92a4605cf38b94be2b0fc15e87",
  "packageLabel": "UI member experience + D-5 status-aware membership rule + D-1 single signup navigation + uncredited target-crossing event + candidate B: recent public additions (W2), repeat policy (W3), guided activity rules (W4), durable history (W6), share + momentum (W7), Champion join QR (W8), kiosk mode (W9) + candidate C: one UI on every screen (pass 1 a45ff2e) and Champion-grade goal creation, secondary states, member sentences (pass 2 80c26ff; head fcb83f7 adds evidence docs only)",
  "sourceAcceptedOn": "2026-09-18",
  "expectedPriorFunctions": 24,
  "_expectedPriorFunctionsNote": "The WSF inventory staging is expected to carry BEFORE this deployment: 24 functions = the 23-function state Package E left (22 pre-Package-E functions plus wsfSetGoalDisplayAuthorization) PLUS wsfGoalRecentAdditions, which staging run 3 (35362036618, 2026-09-18) created before its invoker-policy failure aborted that deploy (its invoker policy was never set). Run 4 (35368478133) correctly refused to deploy against the stale 23 baseline. Evidence for the 24: run 3's deploy log shows exactly the 23 reviewed functions updated and exactly one created; verify-deployment.mjs still requires the post-deploy inventory to equal its 24 expected names (anything else fails as 'present but not expected'), expects wsfGoalRecentAdditions as a created service, and reports its transport. Candidate C (fcb83f7) changes only apps/westayfit app files, browser specs and docs: functions-westayfit and firestore.rules are byte-identical to candidate B, so the prior count stays 24 and the post-deploy inventory must still equal the same 24 names; wsfGoalPulse keeps its nine-field contract. Reviewed per candidate; the next package will have a different prior count."
}
