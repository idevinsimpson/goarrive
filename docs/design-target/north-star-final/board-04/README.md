# Board 04 — Progress

**Status: SELF-CHECKED · independent board review pending.** The `_FINAL` filename is the lock verdict's canonical name for this artifact, not an acceptance status; this is a reconstructed reference / current-build composite, not a recovered original. Locked by PR #365 comment [`5771310017`](https://github.com/idevinsimpson/goarrive/pull/365#issuecomment-5771310017). Status layer from the lock: `/activity` is **accepted build Phase A**; private dated history is **seam only**.

`WE_STAY_FIT_NORTH_STAR_BOARD_04_PROGRESS_FINAL.png` · 2560×3120 (1280×1560 @2x)

## How it was made

```
node scripts/westayfit/north-star/render-board.mjs scripts/westayfit/north-star/board-04.mjs \
  docs/design-target/north-star-final/board-04/WE_STAY_FIT_NORTH_STAR_BOARD_04_PROGRESS_FINAL.png
```

Five frames, all the **accepted Page 4 AFTER evidence** (`docs/design-target/review/page-04-progress/after/`, accepted at aebd1aa), read in place.

## Checked against the lock

| Locked | On the board |
| --- | --- |
| Private to the signed-in member: "Only you can see this…" | The page's own first line, on every frame |
| Summary counts GOALS only, never adds unlike units | "3 goals you have added to · 2 running · 1 finished" |
| WHAT YOU'RE PART OF NOW: exact own credit, RECORDED, goal/community, confirmed shared total + percent + slim track; no Living WE on running rows | `AFTER-rows` |
| WHAT YOU'VE BEEN PART OF keeps finished goals; exactly one Living WE on the most recent finished/reached goal with a real final ratio | `AFTER-rows`: September Push-up Push, REACHED, one mark |
| `Nothing finished yet` means finished rows have not arrived; distinct from `Nothing recorded yet`; goals with ownCredit ≤ 0 are not rows | `AFTER-partial-failure` carries "Nothing finished yet" (2 goals · 0 finished); `AFTER-empty` is "Nothing recorded yet" |
| Loading, whole-screen failure, partial-read note distinct; failure copy preserves record truth | `AFTER-loading`, `AFTER-failure` ("this is the reading, not the record"), `AFTER-partial-failure` |
| No cross-goal totals, own/share ratio, rankings, named figures, community ticker as personal activity; `recorded` governs | None anywhere; stated on the lock panel |
| Private dated history is a SEAM — not built, not authorized; no streak/dates/weekly totals drawn as shipped | The navy panel, naming the contract that would be needed and the review it requires |

## One state inside another frame

The lock names **Nothing finished yet**. The accepted evidence has no standalone frame for it; it is on the board inside the partial-read frame, which the page renders with 0 finished goals. Flagged on the board rather than captured anew, because the accepted evidence is the source this board is built from.
