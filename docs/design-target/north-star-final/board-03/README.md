# Board 03 — Community

**Status: FINAL.** Locked by PR #365 comment [`5771275765`](https://github.com/idevinsimpson/goarrive/pull/365#issuecomment-5771275765). Status layer from the lock: `/community` is **accepted build**; the Join placement inset is **product question / seam only**.

`WE_STAY_FIT_NORTH_STAR_BOARD_03_COMMUNITY_FINAL.png` · 2560×3440 (1280×1720 @2x)

## How it was made

```
node scripts/westayfit/north-star/render-board.mjs scripts/westayfit/north-star/board-03.mjs \
  docs/design-target/north-star-final/board-03/WE_STAY_FIT_NORTH_STAR_BOARD_03_COMMUNITY_FINAL.png
```

Seven frames, all the **accepted Page 3 AFTER evidence** (`docs/design-target/review/page-03-community/after/`, accepted at a8f2ecf), read in place. Every state the lock names has an accepted frame; nothing was captured anew. The Join placement is a labelled question on a navy panel and nowhere a screen.

## Checked against the lock

| Locked | On the board |
| --- | --- |
| `/community` is the identity/membership/switching surface; Home is not redesigned here | The lead line says so; no Home frame appears |
| One current community leads with identity, role/member count, one compact Living WE on the lead goal, additional goal context, anonymous recent movement `{amount, unit, age}` | `AFTER-several-current` |
| Several memberships with none remembered asks explicitly; every row an equal `Choose`; nothing marked CURRENT by convenience | `AFTER-several-nocurrent` |
| Other rows: returned member count + open-goal state/progress or `No goal running`; Switch/Choose text-and-track, no decorative Living WEs | `AFTER-several-current` rows |
| No faces, photos, named movers, reactions, rankings, presence claims, `people moved` counts | None anywhere; stated on the lock panel |
| No-membership state: join via invite link/QR from someone already in, or start a community; no Living WE without a ratio | `AFTER-none` |
| Loading, per-community goal-read failure, no-goal and whole-list failure distinct | `AFTER-loading`, `AFTER-partial-failure`, `AFTER-failure` |
| Current-community failure does not blank other memberships | `AFTER-partial-failure` keeps every row |
| Join placement: accepted build has typed code entry on `/` and links/QR to `/join/[joinCode]`; no Join control on `/community`; a compact secondary treatment is an owner decision seam, not authorized | The navy panel, in those words |

## Nothing was filled from memory

The fixture names and counts on the frames are the accepted fixture's — real membership documents, real counts — and are not real people.
