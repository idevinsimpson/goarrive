# Board 05 — You

**Status: FINAL.** Locked by PR #365 comment [`5771338856`](https://github.com/idevinsimpson/goarrive/pull/365#issuecomment-5771338856). Status layer from the lock: `/you` is **accepted build**; private dated history remains the unbuilt seam documented under Board 04.

`WE_STAY_FIT_NORTH_STAR_BOARD_05_YOU_FINAL.png` · 2560×3440 (1280×1720 @2x)

## How it was made

```
node scripts/westayfit/north-star/render-board.mjs scripts/westayfit/north-star/board-05.mjs \
  docs/design-target/north-star-final/board-05/WE_STAY_FIT_NORTH_STAR_BOARD_05_YOU_FINAL.png
```

Six frames, all the **accepted Page 5 AFTER evidence** (`docs/design-target/review/page-05-you/after/`, accepted at 7e788a9), read in place. Every state the lock names has an accepted frame.

## Checked against the lock

| Locked | On the board |
| --- | --- |
| Identity is one navy field: white wordmark, `YOU`, real display name, member-since month/year, quiet email, Sign out above the fold | Every signed-in frame |
| No photo/avatar, quote/bio, badge, score, rank | None; stated on the lock panel |
| `Where you move` = selected community and returned role/member count; no silent selection | `AFTER-member`; `AFTER-pickcommunity` asks |
| Exactly one dominant Living WE on the selected community's real open-goal ratio | `AFTER-member` |
| YOUR PART labelled apart from "The community is at …"; no own/share ratio, no causal claim | `AFTER-member` |
| Other open and finished goals as compact rows with exact own credit; lead goal not duplicated | `AFTER-member`: ALSO OPEN, FINISHED |
| No cross-unit totals, dated history, weekly counts, streaks, social features, notifications, profile sharing, duplicate leave control, coaching | None; stated on the lock panel |
| Sign out on every resolved signed-in state, including failure | `AFTER-failed` keeps it |
| `pickCommunity`, `noCommunity`, `failed`, `loading`, `signedOut` distinct; signed out exposes no name or email and no member shell | One frame each |

## Fixture note

The display name on the accepted frames is the accepted fixture's (`Devin`, seeded by the Page 5 capture spec) and the email is a generated fixture address. Neither is presented as a real account.
