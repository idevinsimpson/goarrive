/**
 * movements.tsx — Legacy stub (hidden tab)
 *
 * The Movements page has been merged into the unified Build tab.
 * This stub exists because Expo Router requires a file for every
 * registered <Tabs.Screen name="movements" />. It renders nothing.
 */
import React from 'react';
import { View } from 'react-native';

export default function MovementsStub() {
  return <View style={{ flex: 1, backgroundColor: '#0E1117' }} />;
}
