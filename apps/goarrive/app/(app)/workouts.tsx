/**
 * workouts.tsx — Legacy stub (hidden tab)
 *
 * The Workouts page has been merged into the unified Build tab.
 * This stub exists because Expo Router requires a file for every
 * registered <Tabs.Screen name="workouts" />. It renders nothing.
 */
import React from 'react';
import { View } from 'react-native';

export default function WorkoutsStub() {
  return <View style={{ flex: 1, backgroundColor: '#0E1117' }} />;
}
